<p><%= t '.greeting', recipient: @email, locale: locale %></p>

<p><%= t '.instruction', locale: locale %></p>

<p><%= link_to "#{t '.action', locale: locale}", confirmation_url(@resource, confirmation_token: @token, locale: locale) %></p>
