<p><%= t '.greeting', recipient: @resource.email, locale: locale %> </p>

<p><%= t '.message', locale: locale %></p>

<p><%= t '.instruction', locale: locale %></p>

<p><%= link_to "#{t '.action', locale: locale}", unlock_url(@resource, unlock_token: @token, locale: locale) %></p>
