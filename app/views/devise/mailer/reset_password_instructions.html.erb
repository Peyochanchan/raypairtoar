<p><%= t '.greeting', recipient: @resource.email, locale: locale %></p>

<p><%= t '.instruction', locale: locale %></p>

<p><%= link_to "#{t '.action', locale: locale}", edit_password_url(@resource, reset_password_token: @token, locale: locale) %></p>

<p><%= t '.instruction_2', locale: locale %></p>
<p><%= t '.instruction_3', locale: locale %></p>
